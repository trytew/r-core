[package]
name = "r-core"
version = "0.1.0"
edition = "2024"
autotests = false
autobenches = false

[profile.release]
debug = true

[dependencies]
sbi-rt = { version = "0.0.3", features = ["legacy"] }