[package]
name = "r-core"
version = "0.1.0"
edition = "2024"
autotests = false
autobenches = false

[[bin]]
name = "r-core"
test = false
bench = false

[dependencies]
sbi-rt = { version = "0.0.3", features = ["legacy"] }